<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.ygordoring.chat.thelog">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="dev.ygordoring.chat.thelog.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="dev.ygordoring.chat.thelog.presentation.view.activity.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="dev.ygordoring.chat.thelog.presentation.view.activity.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="dev.ygordoring.chat.thelog.presentation.view.activity.SignUpActivity"
            android:screenOrientation="portrait" />
        <activity android:name="dev.ygordoring.chat.thelog.presentation.view.activity.MainActivity" />
        <activity android:name="dev.ygordoring.chat.thelog.presentation.view.activity.PostActivity" />
        <activity android:name="dev.ygordoring.chat.thelog.presentation.view.activity.NewsActivity" />

    </application>

</manifest>